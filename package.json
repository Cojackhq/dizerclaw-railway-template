{
  "name": "dizerclaw-railway-template",
  "version": "1.0.0",
  "description": "OpenClaw on Railway — CoJack template",
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "openclaw": "latest"
  },
  "scripts": {
    "start": "./node_modules/.bin/openclaw gateway start"
  }
}
